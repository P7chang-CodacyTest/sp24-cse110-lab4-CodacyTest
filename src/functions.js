/**
 * 
 * Dummy function that returns sum of two inputs
 * 
 * @param {*} a the second number
 * @param {*} b the first number
 * @returns {int} res the sume of the two numbers
 */
function sum(a, b) {
    res = a + b;
    return res;
}
/**
 * 
 * Dummy function that contains a redundatn variable
 * 
 * @param {*} a the first number
 * @param {*} b the second number
 * @returns {int} res result
 */
function sumR(a, b) {
    R = a;
    res = a + b;
    return res;
}